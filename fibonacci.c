#include<stdio.h>
struct student {
      char name[100];
      char roll[10];
      float cgpa;
   };
   struct student s;
   int main()
   {
       s.r;
      printf("%d %d",s.roll[0],s.roll[1]);
   }
