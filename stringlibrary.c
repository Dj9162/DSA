#include<stdio.h>
// string.h is predefined library used for string 
#include<string.h>
int main()
{
    char s1[]="dhan";
    char s2[]="raj";
    // strcat library is used to concatenate or combine two given strings
    // puts(strcat(s1,s2));
    // printf("Your name is %s",strcat(s1,s2));
    // strlen function is used to show length of a string
    // printf("The length of your name s1 is %d\n ",strlen(s1));
    // printf("The length of your name s2 is %d\n ",strlen(s2));
    // strrev function is used to show reverse of a sring
    // printf("Reverse of s1 is %s\n",strrev(s1));
    // printf("Reverse of s2 is %s\n",strrev(s2));
    // strcpy functionn is used to copy one sjtring into another
    // strcpy(s1,strcat(s1,s2));
    // puts(s1);
    // strcmp function is used to compare two given strings
    // printf("The strcmp for s1,s2 returned %d",strcmp(s1,s2));
    return 0;
}